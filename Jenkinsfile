pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Starting Build'
            }
        }
        stage('Tests') {
            steps {
                echo 'Starting tests'
            }
        }
    }
}